dbt_runner:
  outputs:
    dev:
      dataset: jcdeah_009_bayutrir_staging
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: asia-southeast2
      method: service-account
      priority: interactive
      project: jcdeah-009
      threads: 4
      type: bigquery
  target: dev